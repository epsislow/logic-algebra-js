/* ================= DEVICE MANAGER ================= */

class DeviceManager {
  constructor() {
    this.device = [];
  }
  addDevice(dev, inputs, outputs) {
    // add device to the html
    // add events
  }
}
